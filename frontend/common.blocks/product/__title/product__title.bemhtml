block('product').elem('title')(
  content()(function() {
    var product = this.ctx.product;

    return {
      block: 'link',
      url: '/product/' + product.id,
      content: product.title
    };
  })
);
