block('controller')
    .elem('page')
    .mod('action', 'showbyurl')
    .def()(function() {

    return applyCtx({
        block: 'page-layout',
        mods: {action: 'show'},
        data: this.ctx.data
    });
});
