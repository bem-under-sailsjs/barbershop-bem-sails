block('controller')
    .elem('cart')
    .mod('action', 'show')
    .def()(function() {
    return applyCtx({
        block: 'cart',
        mods: {type: 'show'},
        data: this.ctx.data
    });
});
