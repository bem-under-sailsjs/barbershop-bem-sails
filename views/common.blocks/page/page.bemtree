match(this.ctx.data)
    .def()(function() {
    return applyCtx({
        block: 'page',
        head: [{elem: 'css', url: '/views/desktop.bundles/merged/_merged.css'}],
        title: this.ctx.data.title,
        content: [
            {block: 'head'},
            {block: 'menu'},
            {
                block: 'controller',
                elem: this.ctx.controller,
                mods: {action: this.ctx.action},
                data: this.ctx.data
            },
            {block: 'footer'},
            {elem: 'js', url: '/views/desktop.bundles/merged/_merged.js'}
        ]
    });
});
